!!!
%html
  %head
    %title PayTable
    = stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
  %body
    %header.navbar.navbar-inverse{role: "banner"}
      .navbar-header
        %button#menu-toggler.navbar-toggle{"data-toggle" => "collapse", type: "button"}
          %span.sr-only Toggle navigation
          %span.icon-bar
          %span.icon-bar
          %span.icon-bar
        %a.navbar-brand{href: "index.html"}
          %img{alt: "logo", src: "/assets/logo.png"}/
      %ul.nav.navbar-nav.pull-right.hidden-xs
        %li.notification-dropdown.hidden-xs.hidden-sm
          %a.trigger{href: "#"}
            %i.icon-warning-sign
            %span.count 8
          .pop-dialog
            .pointer.right
              .arrow
              .arrow_border
            .body
              %a.close-icon{href: "#"}
                %i.icon-remove-sign
              .notifications
                %h3 You have 6 new notifications
                %a.item{href: "#"}
                  %i.icon-signin
                  New user registration
                  %span.time
                    %i.icon-time
                    13 min.
                %a.item{href: "#"}
                  %i.icon-signin
                  New user registration
                  %span.time
                    %i.icon-time
                    18 min.
                %a.item{href: "#"}
                  %i.icon-envelope-alt
                  New message from Alejandra
                  %span.time
                    %i.icon-time
                    28 min.
                %a.item{href: "#"}
                  %i.icon-signin
                  New user registration
                  %span.time
                    %i.icon-time
                    49 min.
                %a.item{href: "#"}
                  %i.icon-download-alt
                  New order placed
                  %span.time
                    %i.icon-time
                    1 day.
                .footer
                  %a.logout{href: "#"} View all notifications
        
        %li.dropdown
          %a.dropdown-toggle.hidden-xs.hidden-sm{"data-toggle" => "dropdown", href: "#"}
            Your account
            %b.caret
          %ul.dropdown-menu
            %li
              %a{href: "#{edit_user_registration_path}"} Personal info
            / %li
            /   %a{href: "#"} Account settings
            / %li
            /   %a{href: "#"} Billing
            / %li
            /   %a{href: "#"} Send feedback
        %li.settings.hidden-xs.hidden-sm
          %a{href: "#{edit_user_registration_path}", role: "button"}
            %i.icon-cog
        %li.settings.hidden-xs.hidden-sm
          - if user_signed_in?
            = link_to destroy_user_session_path, method: :delete, role: :button do
              %i.icon-share-alt
    / end navbar
    / sidebar
    = render :partial => "/layouts/sidebar_nav"
    / end sidebar
    / main container
    .content
      / - if user_signed_in?
      /   Logged in as
      /   = succeed "." do
      /     %strong= current_user.username
      /   = link_to 'Edit profile', edit_user_registration_path
      /   |
      /   \#{link_to "Logout", destroy_user_session_path, method: :delete}
      / - else
      /   = link_to "Sign up", new_user_registration_path
      /   |
      /   \#{link_to "Login", new_user_session_path}
      = render :partial => "/layouts/top_stats"
      = bootstrap_flash
      = yield
    